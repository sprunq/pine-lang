[package]
name = "pinec"
version = "0.1.0"
edition = "2021"
description = ""

[dependencies]
base = { path = "../base" }
driver = { path = "../driver" }
messages = { path = "../messages" }
codespan-reporting = { workspace = true }
clap = { version = "4.4.18", features = ["derive"] }
